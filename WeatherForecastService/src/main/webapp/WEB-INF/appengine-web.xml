<appengine-web-app xmlns="http://appengine.google.com/ns/1.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://appengine.google.com/ns/1.0 http://appengine.google.com/ns/1.0/appengine-web.xsd">
    <!-- This is the application ID for your project in Google Cloud -->
    <application>banded-pager-438412-m8</application>

    <!-- Set a version number for your app -->
    <version>1</version>

    <!-- Specify the runtime for your application -->
    <runtime>java21</runtime>

    <!-- Specify the environment (standard) -->
    <env>standard</env>

    <!-- This ensures that the app can handle multiple concurrent requests -->
    <threadsafe>true</threadsafe>

    <!-- Optional: Specify the service if you have multiple services in your app -->
    <!-- <service>default</service> -->

    <!-- Optional: Instance class (e.g., F1, F2) -->
    <!-- <instance-class>F2</instance-class> -->

    <!-- Optional: Automatic scaling settings -->
    <!--
    <automatic-scaling>
        <min-idle-instances>1</min-idle-instances>
        <max-idle-instances>2</max-idle-instances>
        <min-pending-latency>30ms</min-pending-latency>
        <max-pending-latency>500ms</max-pending-latency>
        <max-concurrent-requests>10</max-concurrent-requests>
    </automatic-scaling>
    -->
</appengine-web-app>
